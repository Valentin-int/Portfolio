<form method="post" action="{{ path('admin_project_delete', {'id': project.id}) }}" onsubmit="return confirm('Voulez-vous vraimment supprimer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ project.id) }}">
    <button class="btn">Delete</button>
</form>
