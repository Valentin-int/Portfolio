{% extends "parent/layout_admin.html.twig" %}

{% block stylesheets %}
	{{ parent() }}
	{{ encore_entry_link_tags('new_project') }}
{% endblock %}

{% block body %}
	<div class="justify-content-end mt-5 ms-5">
		<a class="create-button" href="{{ path('admin_project_index') }}">back to list</a>
		{{ include('admin/project/_delete_form.html.twig') }}
	</div>
	<div class="d-flex justify-content-end">
		<div class="form-new-project bg-secondary p-3">
			<h1 class="color-light d-flex justify-content-center mb-4">Modifier un projet</h1>
			{{ include('admin/project/_form.html.twig', {'button_label': 'Update'}) }}
		</div>
	</div>
{% endblock %}
