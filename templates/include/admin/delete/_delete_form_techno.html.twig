<form method="post" action="{{ path('admin_techno_delete', {'id': techno.id}) }}" onsubmit="return confirm('Voulez-vous vraimment supprimer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ techno.id) }}">
    <button class="btn">{% include "include/logo/_cross_red.html.twig" %}</button>
</form>
